export const PROMO_TEXT_ANIMATION_DELAYS = {
  subtitle: 0.3,
} as const;

export const PROMO_MEDIA_QUERIES = {
  reducedMotion: '(prefers-reduced-motion: reduce)',
  videoViewport: '(min-width: 768px)',
} as const;
